services:
  tfsbot:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: tfsbot

    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - ./data:/app/data

    expose:
      - "5050"