"""
Stored Discord forms: models, the JSON
loader, and the SQLCipher form store.

Verification is a reserved form key. It is
seeded from data/forms/verification.json
when that file exists, otherwise from the
built-in questions.
"""
